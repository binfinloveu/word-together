# 驗證紀錄

測試日期：2026-09-14。環境：Windows、Node.js 24、Chrome headless、Playwright；網站由本機 HTTP 伺服器提供。

## 已執行並通過

### 核心行為（6 組測試）

- 單字解析、重複／不完整資料拒絕。
- 0～100 分上下限、同題重送不重複計分。
- 題目不向學生回傳答案欄位、錯字約三題後重現、題型輪替。
- 精熟度優先排序、公開排行榜僅包含名次、名字、精熟度與識別 ID。
- 40 人平均分成 10 組、錯題全隊歸零、舊題版本拒絕、只能一隊獲勝。
- 非隊員、尚未開始或錯誤版本不能更新 Live 分數。

指令：`node --test tests/engine.test.cjs`

### 瀏覽器介面與端到端 WebRTC

- 單字集建立、解析、重排及儲存。
- 翻牌、標記、個人練習連續 8 題判分。
- 320、390、768、1280px 首頁無水平溢出；390px 學習頁無水平溢出。
- 桌面首頁、手機首頁、手機學習、教師 Live、學生 Live、投影畫面截圖檢查。
- 老師與兩位學生使用獨立瀏覽器 context、獨立 localStorage，透過實際公共 PeerJS Cloud 建立 WebRTC 連線。
- 學生提交答案，老師判分，另一位學生看到排行榜更新。
- 鎖房阻止新身份加入。
- 學生重整頁面自動重連，身份與進度維持；不新增排行榜學生。
- 分組、開始 Live、三次答對達標，其他學生畫面停止作答。
- 到時挑戰結果傳送到學生畫面。
- 老師重整、按恢復教室後，學生自動重新連線，原成績保留。
- 教師改名與踢除，即時反映至指定學生。
- 測試中沒有瀏覽器 JavaScript runtime error。

腳本：`tests/browser.cjs`。設定 `TEST_WEBRTC=1` 才執行網路測試，否則只跑本機 UI。需要額外可用的 Playwright 與 Chrome；兩者不是正式網站依賴。

### 40 條實際 WebRTC 連線

腳本：`tests/load.cjs`。

- 老師一個瀏覽器 context，40 個獨立 PeerJS 學生端、各自 token 與 WebRTC DataChannel。
- 40 位學生全部加入成功。
- 40 份答案同批提交；主機全部計分，40 個端點全部取得更新後排行榜。
- 本次量測：從送出到所有端點收到更新約 **190 ms**。這是一次本機網路測試結果，不是延遲保證。
- 分成 10 組後，40 位學生對各組同一版本題目同時提交，每隊只加一次分。

## 實際範圍與尚未驗證

### Live 配樂更新驗證

`tests/music.cjs` 已在 Chrome 使用真實 Web Audio 執行：老師按開始後偵測到非零音訊訊號；音量設定、靜音／恢復、投影持續播放、比賽結束停止，均通過。學生預設靜音，主動開啟後有音訊；離開 Live 及斷線會停止。390px 手機寬度沒有水平溢出，無 JavaScript runtime error。配樂的實體喇叭聆聽仍需在使用者裝置確認。

- WebRTC 測試端點運行在同一台電腦，**未以 40 台實體手機或實際校園網路進行壓力測試**。
- Safari、iOS、Android 實體裝置、校園防火牆與跨 NAT 網路需部署後現場驗證。
- 未測試自架 TURN／PeerServer；預設公共媒合服務目前在此測試環境可連通。
- 語音按鈕使用 SpeechSynthesis；實體裝置聲音與安裝語音需現場確認。
- GitHub Pages 工作流程已提供，尚未在使用者的 GitHub repository 實際發布。

請勿把本機模擬測試當成所有校園網路皆可運作的保證。
